import tkinter as tk

root = tk.Tk()

# Create a frame with a smooth curve border
frame = tk.Frame(root, width=200, height=100, borderwidth=10, relief=tk.RIDGE)
frame.pack()

root.mainloop()
